= image_tag @post.image.url
%br
= @post.caption
= @post.location

= link_to "Edit", edit_post_path(@post)
= link_to "Delete", post_path(@post), :data => {:confirm => "You sure?", :method => "delete"}
